# Copy to a root-owned EnvironmentFile on my-vps and replace placeholders.
# Both destinations must be the Tailscale IPv4 address of vps-contabo.
INVOICE_OAUTH2_UPSTREAM=VPS_CONTABO_TAILSCALE_IP:4180
INVOICE_UI_UPSTREAM=VPS_CONTABO_TAILSCALE_IP:8081
